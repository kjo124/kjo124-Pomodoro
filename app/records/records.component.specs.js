'use strict';

describe('records', function() {
  beforeEach(module('records'));


  // Test the controller
  describe('recordsController', function() {

      it('should should test that records are made and searchable', inject(function($componentController) {

          var ctrl = $componentController('records');

    }));

  });

});
